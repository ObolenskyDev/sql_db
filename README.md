# Retail Sales Analytics (SQL)

![SQL](https://img.shields.io/badge/Language-SQL-orange?style=for-the-badge&logo=postgresql&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/Database-PostgreSQL-blue?style=for-the-badge&logo=postgresql&logoColor=white)

Проект содержит набор SQL-скриптов для анализа продаж магазина электроники. 
Реализована схема базы данных (DDL) и аналитические запросы (DML) для формирования отчетов по выручке и категориям товаров.

## 🛠 Стек
* **PostgreSQL** — диалект SQL.
* **Реляционная модель** — связь таблиц `products` (товары) и `sales` (транзакции).

## 📂 Состав репозитория
* **`schema.sql`** — Создание таблиц, constraints и наполнение тестовыми данными (Seeding).
* **`solutions.sql`** — Примеры бизнес-запросов:
    * Фильтрация и поиск товаров.
    * Агрегация данных (`GROUP BY`, `COUNT`).
    * Расчет финансовой выручки через `JOIN`.
    * Пост-фильтрация отчетов через `HAVING`.

## 🚀 Как запустить
1. Используйте любой PostgreSQL клиент (DBeaver, pgAdmin) или онлайн-песочницу [DB Fiddle](https://www.db-fiddle.com/).
2. Сначала выполните скрипт `schema.sql` для создания структуры.
3. Запускайте запросы из `solutions.sql` для получения аналитики.